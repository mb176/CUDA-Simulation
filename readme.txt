Userguide for the Levy walk simulation

How to use:
Everything is run via two bash scripts, compile.sh and plot.sh: You can modify
the appropriate main.cu file (see list of files below) and then compile via
compile.sh (you have to put the appropriate main into the files_cu). The
